package model;

/**
 * {@link EventType} represents different types of {@link Event}.
 * Possible options are Music, Theatre, Dance, Movie, and Sports.
 */
public enum EventType {
    Music,
    Theatre,
    Dance,
    Movie,
    Sports,
}
